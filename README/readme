запустив проект через редактор кода для начала установим все покеты через терминал "npm i --f"
после установки на терминал пишем npm "start для" для запуска проекта
после этого в browser открываем окна "http://localhost:3000/" или "http://localhost:3001/" 